spring:
  datasource:
    url: jdbc:h2:mem:testdb  # Adjust this URL according to your database
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
